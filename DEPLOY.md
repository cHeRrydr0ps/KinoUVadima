# 🚀 Пошаговая инструкция: Как загрузить сайт на Timeweb

> **Цель:** Перенести ваш сайт из VS Code на сервер Timeweb, чтобы он был доступен по адресу kinouvadima.online

---

## 📌 Что у вас уже есть:
✅ Домен: kinouvadima.online (оплачен)  
✅ Хостинг на Timeweb (оплачен)  
✅ Docker на компьютере  
✅ Проект в VS Code  

---

## ЧАСТЬ 1: Подготовка на вашем компьютере (10 минут)

### Шаг 1: Собрать frontend в VS Code

**Что делаем:** Собираем React приложение в готовые файлы для продакшена.

1. Откройте VS Code в папке вашего проекта
2. Откройте терминал в VS Code: `Терминал → Новый терминал` (или `Ctrl+Ё`)
3. Выполните команды:

```powershell
# Перейти в папку frontend
cd frontend

# Установить все зависимости (если еще не установлено)
npm install

# Собрать production версию
npm run build

# Вернуться в корень проекта
cd ..
```

**Что происходит:** 
- `npm install` скачивает все библиотеки для React
- `npm run build` создает папку `frontend/dist` с готовым сайтом

**Проверка:** В папке `frontend` появилась папка `dist` с файлом `index.html`

---

### Шаг 2: Подготовить файл .env для продакшена

**Что делаем:** Копируем настройки с вашими паролями и ключами.

В терминале VS Code:

```powershell
# Скопировать файл с production настройками
copy .env.production .env
```

**Что происходит:** Файл `.env.production` уже содержит:
- Ваши email настройки (EAS.Suppor@yandex.com)
- Безопасные SECRET_KEY и INTERNAL_SECRET
- URL вашего домена (kinouvadima.online)

**Проверка:** В корне проекта появился файл `.env`

---

### Шаг 3: Подключить Git (если еще не подключен)

**Что делаем:** Создадим Git репозиторий, чтобы загрузить проект на сервер.

**Вариант A: Если у вас уже есть GitHub аккаунт**

В терминале VS Code:

```powershell
# Инициализировать Git (если не сделано)
git init

# Добавить все файлы
git add .

# Сохранить изменения
git commit -m "Готов к деплою"

# Привязать к GitHub (замените на ваш репозиторий)
git remote add origin https://github.com/ваш-username/cinema.git

# Загрузить на GitHub
git push -u origin main
```

**Вариант B: Создать новый репозиторий на GitHub**

1. Зайдите на https://github.com
2. Нажмите "New repository"
3. Название: `cinema` (или любое)
4. Нажмите "Create repository"
5. Скопируйте URL (например: `https://github.com/ваш-username/cinema.git`)
6. В VS Code выполните команды из **Варианта A**, заменив URL

**Важно:** Файл `.env` не загрузится на GitHub (он в .gitignore) - это правильно!

---

## ЧАСТЬ 2: Создание сервера в Timeweb (15 минут)

### Шаг 4: Зайти в панель Timeweb

1. Откройте браузер
2. Перейдите на https://timeweb.com
3. Нажмите **"Войти"** (правый верхний угол)
4. Введите ваш логин и пароль
5. Вы попадете в **Панель управления**

---

### Шаг 5: Создать облачный сервер

**Что делаем:** Заказываем виртуальную машину, на которой будет крутиться сайт.

1. В левом меню найдите **"Cloud"** или **"Серверы"**
2. Нажмите **"Создать сервер"** (синяя кнопка)

**Настройки сервера:**

| Параметр | Что выбрать | Зачем |
|----------|-------------|-------|
| **Операционная система** | Ubuntu 22.04 LTS | Стандартная для Docker |
| **Тариф** | Минимум 2 CPU, 2 GB RAM | Для 7 контейнеров Docker |
| **SSH ключ** | Пропустить (или создать) | Для безопасности, но пока необязательно |
| **Имя сервера** | cinema-prod | Для удобства |

3. Нажмите **"Заказать"**
4. Подождите 2-3 минуты, пока сервер создается

---

### Шаг 6: Получить данные для подключения

После создания сервера:

1. Откройте созданный сервер (кликните на него)
2. Вы увидите:
   - **IP адрес** (например: 185.123.45.67) - запишите!
   - **Пароль root** - нажмите "Показать" и скопируйте

**Сохраните эти данные!** Они нужны для подключения.

---

### Шаг 7: Настроить DNS для домена

**Что делаем:** Связываем домен kinouvadima.online с IP адресом сервера.

1. В панели Timeweb перейдите в **"Домены"**
2. Найдите **kinouvadima.online**
3. Нажмите на него → **"Управление DNS"**
4. Удалите все старые записи (если есть)
5. Добавьте **2 новые A-записи:**

**Запись 1:**
- Тип: **A**
- Имя: **@** (или оставьте пустым)
- Значение: **ваш IP адрес** (например, 185.123.45.67)
- TTL: **3600**

**Запись 2:**
- Тип: **A**
- Имя: **www**
- Значение: **ваш IP адрес** (тот же)
- TTL: **3600**

6. Нажмите **"Сохранить"**

**Ждите 10-30 минут** пока DNS обновится по всему интернету.

---

## ЧАСТЬ 3: Подключение к серверу и установка (20 минут)

### Шаг 8: Подключиться к серверу через SSH

**Что делаем:** Подключаемся к серверу, чтобы выполнять команды на нем.

В **VS Code** откройте терминал и выполните:

```powershell
# Подключиться (замените IP на ваш!)
ssh root@185.123.45.67
```

**Что произойдет:**
1. Появится вопрос: `Are you sure you want to continue connecting (yes/no)?`
   - Напишите: **yes** и Enter
2. Попросит пароль: **введите пароль root** (символы не видны - это нормально)
3. Если всё правильно, увидите приглашение: `root@server:~#`

**Поздравляю! Вы на сервере!** 🎉

---

### Шаг 9: Установить Docker на сервер

**Что делаем:** Устанавливаем Docker - он запустит все части вашего сайта.

**На сервере** выполните команды по очереди:

```bash
# 1. Скачать скрипт установки Docker
curl -fsSL https://get.docker.com -o get-docker.sh

# 2. Запустить установку (займет 2-3 минуты)
sh get-docker.sh

# 3. Установить Docker Compose
apt install docker-compose-plugin -y

# 4. Проверить что всё установилось
docker --version
docker compose version
```

**Результат:** Вы должны увидеть версии Docker (например, Docker version 24.0.7)

---

### Шаг 10: Загрузить проект на сервер

**Что делаем:** Скачиваем ваш код с GitHub на сервер.

```bash
# 1. Установить Git (если не установлен)
apt install git -y

# 2. Создать папку для проекта
mkdir -p /var/www

# 3. Перейти в неё
cd /var/www

# 4. Скачать проект с GitHub (замените URL!)
git clone https://github.com/ваш-username/cinema.git cinema

# 5. Перейти в папку проекта
cd cinema

# 6. Посмотреть что всё загрузилось
ls -la
```

**Проверка:** Вы должны увидеть папки: `frontend`, `services`, `gateway` и файлы: `docker-compose.prod.yml`

---

### Шаг 11: Создать файл .env на сервере

**Проблема:** Файл `.env` не загрузился на GitHub (он в .gitignore).

**Решение:** Создадим его вручную на сервере.

```bash
# Открыть редактор nano
nano .env
```

**В редакторе nano вставьте содержимое файла `.env.production`:**

Скопируйте содержимое из VS Code (файл `.env.production`) и вставьте в nano:
- **Вставить:** Правая кнопка мыши или `Shift+Insert`

**Сохранить и выйти:**
- Нажмите `Ctrl+O` (сохранить)
- Нажмите `Enter` (подтвердить имя файла)
- Нажмите `Ctrl+X` (выйти)

**Проверка:**
```bash
# Посмотреть что файл создан
cat .env | head -20
```

Вы должны увидеть ваши настройки.

---

### Шаг 12: Запустить сайт!

**Что делаем:** Запускаем Docker контейнеры - это поднимет весь сайт.

```bash
# Запустить все сервисы
docker compose -f docker-compose.prod.yml up -d --build
```

**Что происходит:**
- Скачиваются образы (PostgreSQL, Redis, Nginx) - 3 минуты
- Собираются ваши сервисы (Python FastAPI) - 10 минут
- Запускаются все 13 контейнеров

**Следить за процессом:**
```bash
# Показать логи
docker compose -f docker-compose.prod.yml logs -f
```

Нажмите `Ctrl+C` чтобы выйти (контейнеры продолжат работать).

**Проверка:**
```bash
# Статус контейнеров
docker compose -f docker-compose.prod.yml ps
```

Все должны быть **"Up"** (запущены).

---

### Шаг 13: Создать таблицы в базах данных

**Что делаем:** Применяем миграции - это создаст таблицы для пользователей, фильмов и т.д.

```bash
# База пользователей (auth)
docker compose -f docker-compose.prod.yml exec auth_service_prod alembic upgrade head

# База контента (admin)
docker compose -f docker-compose.prod.yml exec admin_service_prod alembic upgrade head

# База платежей (payment)
docker compose -f docker-compose.prod.yml exec payment_service_prod alembic upgrade head
```

После каждой команды вы увидите: `Running upgrade ...` - это нормально.

---

## ЧАСТЬ 4: Проверка и SSL (10 минут)

### Шаг 14: Проверить что сайт работает (без HTTPS)

**На вашем компьютере** откройте браузер:

👉 Перейдите на: **http://kinouvadima.online** (без https!)

**Что вы должны увидеть:**
- ✅ Главная страница сайта
- ✅ Можно нажать "Регистрация"
- ✅ Видно каталог фильмов

**Если не открывается:**
1. Подождите 10 минут (DNS еще обновляется)
2. Проверьте DNS: `nslookup kinouvadima.online` в PowerShell
3. Посмотрите логи на сервере: `docker compose -f docker-compose.prod.yml logs gateway_prod`

---

### Шаг 15: Получить SSL сертификат (HTTPS)

**Что делаем:** Получаем бесплатный сертификат от Let's Encrypt.

**На сервере:**

```bash
# 1. Сначала создать HTTP конфигурацию
cd /var/www/cinema
cp gateway/app.http.conf gateway/app.prod.conf

# 2. Перезапустить nginx
docker compose -f docker-compose.prod.yml restart gateway_prod

# 3. Получить сертификат (замените email!)
docker compose -f docker-compose.prod.yml run --rm certbot certonly \
  --webroot --webroot-path=/var/www/certbot \
  -d kinouvadima.online -d www.kinouvadima.online \
  --email stallionofwolves@yandex.ru \
  --agree-tos --no-eff-email
```

**Результат:** Увидите сообщение `Successfully received certificate`

```bash
# 4. Вернуть HTTPS конфигурацию
cp gateway/app.prod.conf.original gateway/app.prod.conf
# Или вручную замените содержимое файла на версию с SSL

# 5. Перезапустить nginx
docker compose -f docker-compose.prod.yml restart gateway_prod
```

---

### Шаг 16: Проверить HTTPS

**На вашем компьютере** откройте браузер:

👉 Перейдите на: **https://kinouvadima.online** (с https!)

**Проверьте:**
- ✅ Зеленый замочек 🔒 в адресной строке
- ✅ Сайт открывается
- ✅ При попытке зайти на http:// автоматически перенаправляет на https://

---

## ЧАСТЬ 5: Создание администратора (5 минут)

### Шаг 17: Зарегистрироваться на сайте

1. Откройте https://kinouvadima.online
2. Нажмите **"Регистрация"**
3. Заполните форму:
   - Email: **stallionofwolves@yandex.ru** (ваш email)
   - Придумайте пароль
   - Остальные поля
4. Нажмите "Зарегистрироваться"

---

### Шаг 18: Сделать себя администратором

**На сервере:**

```bash
# Подключиться к базе данных
docker compose -f docker-compose.prod.yml exec db_auth_prod psql -U user users_auth_db
```

Откроется консоль PostgreSQL.

**Выполните SQL команды:**

```sql
-- Посмотреть всех пользователей
SELECT id, email, is_admin FROM users;

-- Сделать себя админом (замените email если нужно)
UPDATE users SET is_admin=true WHERE email='stallionofwolves@yandex.ru';

-- Проверить
SELECT id, email, is_admin FROM users;

-- Выйти
\q
```

**Проверка:**
1. Обновите страницу сайта
2. Войдите в аккаунт
3. Вы должны увидеть **"Админ панель"** в меню

---

## 🎉 ГОТОВО!

Ваш сайт работает на: **https://kinouvadima.online**

### Что теперь можно делать:

✅ Заходить на сайт с любого устройства  
✅ Регистрировать пользователей  
✅ Загружать фильмы через админ панель  
✅ Получать email уведомления  

---

## 📝 Полезные команды для управления

**Посмотреть логи всех сервисов:**
```bash
cd /var/www/cinema
docker compose -f docker-compose.prod.yml logs -f
```

**Перезапустить сайт:**
```bash
docker compose -f docker-compose.prod.yml restart
```

**Остановить сайт:**
```bash
docker compose -f docker-compose.prod.yml down
```

**Запустить заново:**
```bash
docker compose -f docker-compose.prod.yml up -d
```

**Обновить код с GitHub:**
```bash
cd /var/www/cinema
git pull
docker compose -f docker-compose.prod.yml up -d --build
```

---

## 🆘 Что делать если что-то сломалось?

1. **Посмотреть логи:**
   ```bash
   docker compose -f docker-compose.prod.yml logs -f
   ```

2. **Проверить что все контейнеры работают:**
   ```bash
   docker compose -f docker-compose.prod.yml ps
   ```

3. **Перезапустить всё:**
   ```bash
   docker compose -f docker-compose.prod.yml restart
   ```

4. **Написать мне!** 😊

---

## 📞 Контакты

- **Email:** stallionofwolves@yandex.ru
- **Поддержка Timeweb:** https://timeweb.com/ru/support

---

**Поздравляю с запуском сайта!** 🚀🎬
